router rip 
    network 3.0.0.0/8
    redistribute bgp

debug bgp keepalive
debug bgp updates in 
debug bgp updates out

router bgp 30
    no bgp network import-check
    no bgp ebgp-requires-policy

    ! neighbor 20.0.0.13 remote-as 20
    ! neighbor 20.0.0.13 description AS2(as2r1)
    neighbor 3.0.0.2 remote-as 30
    neighbor 3.0.0.2 description AS3(as3r2)

    network 3.0.0.0/24
    network 3.1.0.0/24
    network 3.2.0.0/24
    network 20.0.0.12/30


log file /var/log/frr/frr.log

