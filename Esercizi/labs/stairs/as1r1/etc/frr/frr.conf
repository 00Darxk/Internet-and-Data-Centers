router rip
    network 1.0.0.0/24
    redistribute connected
    redistribute bgp

router bgp 1
    no bgp network import-check
    no bgp ebgp-requires-policy
    
    neighbor 1.0.0.2 remote-as 1
    neighbor 1.0.0.2 description AS1(as1r2)
    neighbor 10.0.0.2 remote-as 2
    neighbor 10.0.0.2 description AS2(as2r1)

    network 1.0.0.0/24
    network 10.0.0.0/30

log file /var/log/frr/frr.log


